{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [

        {
            "name": "mass estimator",
            "type": "debugpy",
            "request": "launch",
            // Instead of `${file}`, specify the program you want to run:
            "program": "${workspaceFolder}/diff_obj/mass_estimator_U_density.py",
            "console": "integratedTerminal",
            // If you need to pass arguments (e.g. --log), include them here:
            "args": [
                "--log"
            ]
        },
        {
            "name": "mass estimator lego",
            "type": "debugpy",
            "request": "launch",
            // Instead of `${file}`, specify the program you want to run:
            "program": "${workspaceFolder}/diff_obj/mass_estimator_Lego.py",
            "console": "integratedTerminal",
            // If you need to pass arguments (e.g. --log), include them here:
            "args": [
                "--log"
            ]
        },
        {
            "name": "mass estimator domino",
            "type": "debugpy",
            "request": "launch",
            // Instead of `${file}`, specify the program you want to run:
            "program": "${workspaceFolder}/diff_obj/mass_estimator_domino.py",
            "console": "integratedTerminal",
            // If you need to pass arguments (e.g. --log), include them here:
            "args": [
                "--log"
            ]
        },
        {
            "name": "mass estimator A",
            "type": "debugpy",
            "request": "launch",
            // Instead of `${file}`, specify the program you want to run:
            "program": "${workspaceFolder}/diff_obj/mass_estimator_a.py",
            "console": "integratedTerminal",
            // If you need to pass arguments (e.g. --log), include them here:
            "args": [
                "--log"
            ]
        },
        {
            "name": "mass estimator U2",
            "type": "debugpy",
            "request": "launch",
            // Instead of `${file}`, specify the program you want to run:
            "program": "${workspaceFolder}/diff_obj/mass_estimator_U_density2.py",
            "console": "integratedTerminal",
            // If you need to pass arguments (e.g. --log), include them here:
            "args": [
                "--log"
            ]
        },
        {
            "name": "mass estimator U3",
            "type": "debugpy",
            "request": "launch",
            // Instead of `${file}`, specify the program you want to run:
            "program": "${workspaceFolder}/diff_obj/mass_estimator_U_density3.py",
            "console": "integratedTerminal",
            // If you need to pass arguments (e.g. --log), include them here:
            "args": [
                "--log"
            ]
        },
    ]
}